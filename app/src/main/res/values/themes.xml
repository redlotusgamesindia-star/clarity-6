<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Post-splash theme. Compose (ClarityTheme) owns all real theming;
         this only prevents a white flash before the first frame. -->
    <style name="Theme.Clarity" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="Theme.Clarity.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.Clarity</item>
    </style>
</resources>
